#include <stdio.h>

/**
 * main - Entry point
 * Program writes to the standard output with printf function
 * Return: return 0 after executing the program
 * Program compiles without warning when, with the -Wall gcc option
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
