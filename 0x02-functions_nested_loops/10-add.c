#include "main.h"
#include <stdio.h>

/**
* add -prints add two numbers
*
* @i: argument with variable integer
* @k: argument with variable integer
* Return: Always 0.
*/

int add(int i, int k)
{
	return (i + k);
}
