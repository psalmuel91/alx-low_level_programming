#include "main.h"
#include <stdio.h>

/**
 * main - Name of the program printed
 * @argc: Number of CL args
 * @argv: Array containing the program CL arg
 * Return: return (0) success
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
